# AI Code Assist Nest Demo

A minimal NestJS demo that shows a Planner API with a mock LLM and an executor that applies file changes to a fixture repo.

## Run locally

```bash
cd ai-code-assist-nest-demo
npm install
npm run dev

