/** Default instruction for generating plain text files. */
export const TEXT_INSTRUCTION = `
Generate plain text content.

Ensure:
- The output is purely text, without any formatting like Markdown, HTML, or JSON.
- Newlines are used for separation where appropriate.

Provide ONLY the plain text content, without any surrounding markdown code blocks or additional text.
`;
