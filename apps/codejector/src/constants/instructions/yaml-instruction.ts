/** Default instruction for generating YAML files. */
export const YAML_INSTRUCTION = `
Generate valid YAML content.

Ensure:
- Correct YAML syntax, including indentation and structure.
- Use of appropriate data types (strings, numbers, booleans, lists, dictionaries).
- Comments where necessary for clarity.

Provide ONLY the YAML content, without any surrounding markdown code blocks or additional text.
`;
