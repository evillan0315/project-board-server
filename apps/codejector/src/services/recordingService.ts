// src/services/recordingService.ts

export const recordingService = {
  // Add methods to interact with the recording API here
  // Example:
  // startRecording: async () => {
  //   return await recordingApi.startRecording();
  // },
};
