# Kanban Board

A simple Kanban board application built with React, Vite, and Material UI.

## Getting Started

1.  Install dependencies:

    ```bash
    npm install
    ```

2.  Run the development server:

    ```bash
    npm run dev
    ```